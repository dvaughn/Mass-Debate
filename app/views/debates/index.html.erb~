<div class="container">
  <div class="page-header">
    <h1>Debate Lobby<small>Topic View</small></h1>
  </div>

  <h3>Topics:</h3>
  <button class="btn btn-primary" type="button" data-toggle="modal" data-target="#newSiteModal">Register New Site</button>
  <p>
    <table class="table table-striped table-hover">
      <tr>
        <th>Site URL</th>
        <th></th>
      </tr>
 
      <% @sites.each do |site| %>

      <tr>
        <td><%= site.hostname %></td>
        <td>

          <%= form_for site, :url => { :controller => :sites, :action=> :index }, :html => { :class => "login_form", :method => "POST" } do |f| %>
            <%= f.hidden_field :site_id, :value => site.id %>
            <%= f.submit "View", :class => "btn btn-inverse" %>
          <% end %>

        </td>
      </tr>

      <% end %>

    </table>
  </p>
</div>
